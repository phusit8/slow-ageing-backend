<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ปฏิทินรายเดือน - Slow Aging</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://code.iconify.design/3/3.1.0/iconify.min.js"></script>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600;700;800&display=swap');
    body { font-family: 'Prompt', sans-serif; }
  </style>
</head>
<body class="bg-[#f5f7f5]">
  <div class="flex flex-col min-h-screen w-full">
    <div class="w-full max-w-md sm:max-w-lg md:max-w-xl mx-auto flex-1 flex flex-col px-5 pt-6 pb-12">

      <!-- Header -->
      <div class="relative flex items-center justify-center mb-6">
        <a href="/home" class="absolute left-0 p-2 -ml-2 text-black cursor-pointer hover:bg-stone-200/60 rounded-2xl transition-colors">
          <span class="iconify text-3xl sm:text-4xl" data-icon="mdi:arrow-left"></span>
        </a>
        <h1 class="text-3xl sm:text-4xl font-extrabold text-[#1a2e1a]">ปฏิทินรายเดือน</h1>
      </div>

      <!-- เดือน/ปี Navigator -->
      <div class="flex items-center justify-center gap-4 mb-5 bg-white p-3 rounded-2xl border border-stone-200 shadow-sm">
        <button id="prevBtn" class="p-2 cursor-pointer text-stone-600 hover:text-black rounded-xl hover:bg-stone-100">
          <span class="iconify text-3xl sm:text-4xl" data-icon="mdi:chevron-left"></span>
        </button>
        <span id="monthYearText" class="text-xl sm:text-2xl font-extrabold text-[#1a2e1a] min-w-[180px] text-center">
          <!-- Inject by JS -->
        </span>
        <button id="nextBtn" class="p-2 cursor-pointer text-stone-600 hover:text-black rounded-xl hover:bg-stone-100">
          <span class="iconify text-3xl sm:text-4xl" data-icon="mdi:chevron-right"></span>
        </button>
      </div>

      <!-- การ์ดความสม่ำเสมอ -->
      <div class="bg-white border border-stone-200 rounded-3xl p-6 shadow-sm mb-5">
        <h2 class="text-2xl sm:text-3xl font-extrabold text-[#1a2e1a] mb-2">ความสม่ำเสมอของเดือนนี้</h2>
        <p class="text-4xl sm:text-5xl font-extrabold text-[#1a2e1a] mb-3"><span id="consistencyText" class="text-[#2d7a3a]">0</span>%</p>
        <div class="w-full h-4 rounded-full bg-stone-100 overflow-hidden mb-2">
          <div id="consistencyBar" class="h-full rounded-full transition-all duration-500" style="width: 0%; background: #2d7a3a;"></div>
        </div>
        <p id="consistencyDesc" class="text-base sm:text-lg text-stone-600 font-bold">
          ทำได้ 0 วัน จาก 0 วัน
        </p>
      </div>

      <!-- ปฏิทิน -->
      <div class="bg-white border border-stone-200 rounded-3xl p-5 sm:p-6 shadow-sm mb-5">
        <!-- วันในสัปดาห์ -->
        <div class="grid grid-cols-7 mb-3">
          <div class="text-center text-lg sm:text-xl font-extrabold text-stone-600 py-1">อา</div>
          <div class="text-center text-lg sm:text-xl font-extrabold text-stone-600 py-1">จ</div>
          <div class="text-center text-lg sm:text-xl font-extrabold text-stone-600 py-1">อ</div>
          <div class="text-center text-lg sm:text-xl font-extrabold text-stone-600 py-1">พ</div>
          <div class="text-center text-lg sm:text-xl font-extrabold text-stone-600 py-1">พฤ</div>
          <div class="text-center text-lg sm:text-xl font-extrabold text-stone-600 py-1">ศ</div>
          <div class="text-center text-lg sm:text-xl font-extrabold text-stone-600 py-1">ส</div>
        </div>

        <!-- ตารางวันที่ -->
        <div id="calendarGrid" class="grid grid-cols-7 gap-y-2.5">
          <!-- Inject by JS -->
        </div>
      </div>

      <!-- Legend -->
      <div class="flex items-center justify-center gap-6 mb-6 bg-white p-4 rounded-2xl border border-stone-200 shadow-sm">
        <div class="flex items-center gap-2">
          <div class="w-4 h-4 rounded-full" style="background: #2d7a3a;"></div>
          <span class="text-base sm:text-lg text-stone-700 font-bold">ทำครบ</span>
        </div>
        <div class="flex items-center gap-2">
          <div class="w-4 h-4 rounded-full" style="background: #f59e0b;"></div>
          <span class="text-base sm:text-lg text-stone-700 font-bold">ทำบางส่วน</span>
        </div>
        <div class="flex items-center gap-2">
          <div class="w-4 h-4 rounded-full" style="background: #9ca3af;"></div>
          <span class="text-base sm:text-lg text-stone-700 font-bold">ไม่ได้ทำ</span>
        </div>
      </div>

      <!-- สถิติเดือนนี้ -->
      <div class="bg-white border border-stone-200 rounded-3xl p-6 shadow-sm">
        <h2 class="text-2xl sm:text-3xl font-extrabold text-[#1a2e1a] mb-4">สถิติเดือนนี้</h2>
        <div class="grid grid-cols-3 divide-x-2 divide-stone-200">
          <div class="flex flex-col items-center px-1">
            <span id="statDone" class="text-3xl sm:text-4xl font-extrabold text-[#2d7a3a]">0</span>
            <span class="text-base text-stone-600 font-bold">วัน</span>
            <span class="text-base font-bold text-[#2d7a3a] mt-0.5">ทำครบ</span>
          </div>
          <div class="flex flex-col items-center px-1">
            <span id="statPartial" class="text-3xl sm:text-4xl font-extrabold text-amber-500">0</span>
            <span class="text-base text-stone-600 font-bold">วัน</span>
            <span class="text-base font-bold text-amber-600 mt-0.5">ทำบางส่วน</span>
          </div>
          <div class="flex flex-col items-center px-1">
            <span id="statMissed" class="text-3xl sm:text-4xl font-extrabold text-stone-500">0</span>
            <span class="text-base text-stone-600 font-bold">วัน</span>
            <span class="text-base font-bold text-stone-500 mt-0.5">ไม่ได้ทำ</span>
          </div>
        </div>
      </div>

    </div>
  </div>

  <script>
    const MONTH_NAMES = [
      "มกราคม", "กุมภาพันธ์", "มีนาคม", "เมษายน", "พฤษภาคม", "มิถุนายน",
      "กรกฎาคม", "สิงหาคม", "กันยายน", "ตุลาคม", "พฤศจิกายน", "ธันวาคม",
    ];

    let currentMonth = 7; // 7 = สิงหาคม
    let currentYear = 2569;

    // Mock data
    const calendarData = [
      { day: 1, status: null }, { day: 2, status: "done" }, { day: 3, status: null },
      { day: 4, status: null }, { day: 5, status: null }, { day: 6, status: "done" },
      { day: 7, status: "done" }, { day: 8, status: "done" }, { day: 9, status: "done" },
      { day: 10, status: null }, { day: 11, status: null }, { day: 12, status: null },
      { day: 13, status: null }, { day: 14, status: null }, { day: 15, status: "done" },
      { day: 16, status: "done" }, { day: 17, status: null }, { day: 18, status: null },
      { day: 19, status: null }, { day: 20, status: "done" }, { day: 21, status: "done" },
      { day: 22, status: null }, { day: 23, status: "partial" }, { day: 24, status: null },
      { day: 25, status: null }, { day: 26, status: null }, { day: 27, status: "done" },
      { day: 28, status: "partial" }, { day: 29, status: "done" }, { day: 30, status: "partial" },
      { day: 31, status: null },
    ];

    function renderCalendar() {
      // อัปเดต Header
      document.getElementById("monthYearText").innerText = `${MONTH_NAMES[currentMonth]} ${currentYear}`;

      const realYear = currentYear - 543;
      const firstDayOfWeek = new Date(realYear, currentMonth, 1).getDay();
      const daysInPrevMonth = new Date(realYear, currentMonth, 0).getDate();

      // สร้าง Grid Data
      const gridCells = [];
      for (let i = firstDayOfWeek - 1; i >= 0; i--) {
        gridCells.push({ day: daysInPrevMonth - i, inMonth: false, status: null });
      }
      calendarData.forEach((d) => {
        gridCells.push({ day: d.day, inMonth: true, status: d.status });
      });
      const remainingCells = 7 - (gridCells.length % 7);
      if (remainingCells < 7) {
        for (let i = 1; i <= remainingCells; i++) {
          gridCells.push({ day: i, inMonth: false, status: null });
        }
      }

      // วาดปฏิทิน
      const calendarGrid = document.getElementById("calendarGrid");
      calendarGrid.innerHTML = "";
      gridCells.forEach(cell => {
        let bgColor = "transparent";
        let textColor = cell.inMonth ? "#1a2e1a" : "#d0d0d0";
        let fontWeight = "600";

        if (cell.inMonth) {
          if (cell.status === "done") {
            bgColor = "#2d7a3a"; textColor = "#ffffff"; fontWeight = "800";
          } else if (cell.status === "partial") {
            bgColor = "#f59e0b"; textColor = "#ffffff"; fontWeight = "800";
          } else if (cell.status === "missed") {
            bgColor = "#9ca3af"; textColor = "#ffffff"; fontWeight = "800";
          }
        }

        calendarGrid.innerHTML += `
          <div class="flex items-center justify-center py-1">
            <div class="w-11 h-11 sm:w-12 sm:h-12 rounded-full flex items-center justify-center text-lg sm:text-xl shadow-xs" style="background: ${bgColor}; color: ${textColor}; font-weight: ${fontWeight};">
              ${cell.day}
            </div>
          </div>
        `;
      });

      // อัปเดตสถิติ
      const doneCount = calendarData.filter((d) => d.status === "done").length;
      const partialCount = calendarData.filter((d) => d.status === "partial").length;
      const missedCount = calendarData.filter((d) => d.status === "missed").length;
      const totalDays = calendarData.length;
      const consistencyPercent = Math.round(((doneCount + partialCount * 0.5) / totalDays) * 100);

      document.getElementById("consistencyText").innerText = consistencyPercent;
      document.getElementById("consistencyDesc").innerText = `ทำได้ ${doneCount} วัน จากทั้งหมด ${totalDays} วัน`;
      document.getElementById("statDone").innerText = doneCount;
      document.getElementById("statPartial").innerText = partialCount;
      document.getElementById("statMissed").innerText = missedCount || (totalDays - doneCount - partialCount);
      
      setTimeout(() => {
        document.getElementById("consistencyBar").style.width = `${consistencyPercent}%`;
      }, 100);
    }

    document.getElementById("prevBtn").addEventListener("click", () => {
      if (currentMonth === 0) {
        currentMonth = 11;
        currentYear--;
      } else {
        currentMonth--;
      }
      renderCalendar();
    });

    document.getElementById("nextBtn").addEventListener("click", () => {
      if (currentMonth === 11) {
        currentMonth = 0;
        currentYear++;
      } else {
        currentMonth++;
      }
      renderCalendar();
    });

    renderCalendar();
  </script>
</body>
</html>
